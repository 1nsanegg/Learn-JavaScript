let person = {
    name: "Trevor",
    age: 23,
    country: "Vietnam"
}

function logData(person) {
    console.log(`${person.name} is ${person.age} years old and lives in ${person.country}`)
}

logData(person)