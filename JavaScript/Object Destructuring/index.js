const dreamHoliday = {
  destination: "Dalat",
  activity: "Drink coffee",
  accommodation: "Hotel",
  companion: "My friend",
};

const { destination, activity, accommodation, companion } = dreamHoliday;

message = `I would love to go to ${destination} to ${activity}. I'd sleep in a ${accommodation} and hangout with ${companion} all day`;

console.log(message);
